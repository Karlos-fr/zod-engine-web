/**
 * Ported from Zod Engine.
 * Upstream: zod_engine_private.h
 * Symbols: LEGAL_COPYRIGHT, VER_RELEASE, ORIGINAL_FILENAME, VER_STRING,
 * PRODUCT_VERSION, FILE_DESCRIPTION, COMPANY_NAME, FILE_VERSION, VER_MINOR,
 * LEGAL_TRADEMARKS, VER_BUILD, PRODUCT_NAME, ZOD_ENGINE_PRIVATE_H,
 * INTERNAL_NAME, VER_MAJOR
 */

/**
 * Adaptation of upstream `ZOD_ENGINE_PRIVATE_H`.
 * Role: Marks the TypeScript module boundary for upstream `zod_engine_private.h`.
 * Ledger: MAC-D9FEDE
 * Upstream: zod_engine_private.h:5
 */
export const ZOD_ENGINE_PRIVATE_HEADER_GUARD_PORTED = true;

/**
 * Adaptation of upstream `VER_MAJOR`.
 * Role: Stores the major component of the product version.
 * Ledger: MAC-F62E20
 * Upstream: zod_engine_private.h:9
 */
export const ZOD_ENGINE_VERSION_MAJOR = 0;

/**
 * Adaptation of upstream `LEGAL_COPYRIGHT`.
 * Role: Stores the product copyright metadata.
 * Ledger: MAC-002491
 * Upstream: zod_engine_private.h:16
 */
export const ZOD_ENGINE_LEGAL_COPYRIGHT = "";

/**
 * Adaptation of upstream `VER_RELEASE`.
 * Role: Stores the release component of the product version.
 * Ledger: MAC-0A4225
 * Upstream: zod_engine_private.h:11
 */
export const ZOD_ENGINE_VERSION_RELEASE = 1;

/**
 * Adaptation of upstream `VER_BUILD`.
 * Role: Stores the build component of the product version.
 * Ledger: MAC-C05542
 * Upstream: zod_engine_private.h:12
 */
export const ZOD_ENGINE_VERSION_BUILD = 140;

/**
 * Adaptation of upstream `ORIGINAL_FILENAME`.
 * Role: Stores the original executable filename metadata.
 * Ledger: MAC-0D36CD
 * Upstream: zod_engine_private.h:18
 */
export const ZOD_ENGINE_ORIGINAL_FILENAME = "";

/**
 * Adaptation of upstream `VER_STRING`.
 * Role: Stores the full product version string.
 * Ledger: MAC-275440
 * Upstream: zod_engine_private.h:8
 */
export const ZOD_ENGINE_VERSION_STRING = "0.1.1.140";

/**
 * Adaptation of upstream `PRODUCT_VERSION`.
 * Role: Stores the product version metadata string.
 * Ledger: MAC-5CC8F1
 * Upstream: zod_engine_private.h:20
 */
export const ZOD_ENGINE_PRODUCT_VERSION = "";

/**
 * Adaptation of upstream `FILE_DESCRIPTION`.
 * Role: Stores the executable description metadata.
 * Ledger: MAC-753812
 * Upstream: zod_engine_private.h:14
 */
export const ZOD_ENGINE_FILE_DESCRIPTION =
  "An open source engine for the game Z by The Bitmap Brothers";

/**
 * Adaptation of upstream `COMPANY_NAME`.
 * Role: Stores the company name metadata.
 * Ledger: MAC-83E500
 * Upstream: zod_engine_private.h:12
 */
export const ZOD_ENGINE_COMPANY_NAME = "Nighsoft";

/**
 * Adaptation of upstream `FILE_VERSION`.
 * Role: Stores the file version metadata string.
 * Ledger: MAC-89A114
 * Upstream: zod_engine_private.h:13
 */
export const ZOD_ENGINE_FILE_VERSION = "";

/**
 * Adaptation of upstream `INTERNAL_NAME`.
 * Role: Stores the internal executable name metadata.
 * Ledger: MAC-E10446
 * Upstream: zod_engine_private.h:15
 */
export const ZOD_ENGINE_INTERNAL_NAME = "";

/**
 * Adaptation of upstream `VER_MINOR`.
 * Role: Stores the minor component of the product version.
 * Ledger: MAC-9BBFC3
 * Upstream: zod_engine_private.h:10
 */
export const ZOD_ENGINE_VERSION_MINOR = 1;

/**
 * Adaptation of upstream `LEGAL_TRADEMARKS`.
 * Role: Stores the product trademark metadata.
 * Ledger: MAC-BFD033
 * Upstream: zod_engine_private.h:17
 */
export const ZOD_ENGINE_LEGAL_TRADEMARKS = "";

/**
 * Adaptation of upstream `PRODUCT_NAME`.
 * Role: Stores the product name metadata.
 * Ledger: MAC-D80FA3
 * Upstream: zod_engine_private.h:19
 */
export const ZOD_ENGINE_PRODUCT_NAME = "The Zod Engine";
