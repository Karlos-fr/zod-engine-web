/**
 * Ported from Zod Engine.
 * Upstream: main.h, xgetopt.h
 * Symbols: _MAIN_H_, XGETOPT_H
 */

/**
 * Adaptation of upstream `_MAIN_H_`.
 * Role: Marks the TypeScript module boundary for upstream `main.h`.
 * Ledger: MAC-DD84CF
 * Upstream: main.h:2
 */
export const MAIN_OPTIONS_HEADER_GUARD_PORTED = true;

/**
 * Adaptation of upstream `XGETOPT_H`.
 * Role: Marks the TypeScript module boundary for the future Windows getopt shim port.
 * Ledger: MAC-2BEE1A
 * Upstream: xgetopt.h:20
 */
export const XGETOPT_HEADER_GUARD_PORTED = true;
