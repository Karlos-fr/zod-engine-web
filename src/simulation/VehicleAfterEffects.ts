/**
 * Upstream: vapc.cpp, vcrane.cpp, vheavy.cpp, vjeep.cpp, vlight.cpp, vmedium.cpp,
 * vmissilelauncher.cpp
 */

/**
 * Port of upstream `VAPC::DoAfterEffects`.
 * Role: Hook for APC-specific post-render effects; upstream has no behavior.
 * Upstream: vapc.cpp:211-217
 */
export function doApcAfterEffects(
  map: unknown,
  destination: unknown,
  shiftX: number,
  shiftY: number,
): void {
  void map;
  void destination;
  void shiftX;
  void shiftY;
}

/**
 * Port of upstream `VCrane::DoAfterEffects`.
 * Role: Hook for crane-specific post-render effects; upstream has no behavior.
 * Upstream: vcrane.cpp:231-237
 */
export function doCraneAfterEffects(
  map: unknown,
  destination: unknown,
  shiftX: number,
  shiftY: number,
): void {
  void map;
  void destination;
  void shiftX;
  void shiftY;
}

/**
 * Port of upstream `VHeavy::DoAfterEffects`.
 * Role: Hook for heavy-tank-specific post-render effects; upstream has no behavior.
 * Upstream: vheavy.cpp:205-211
 */
export function doHeavyAfterEffects(
  map: unknown,
  destination: unknown,
  shiftX: number,
  shiftY: number,
): void {
  void map;
  void destination;
  void shiftX;
  void shiftY;
}

/**
 * Port of upstream `VJeep::DoAfterEffects`.
 * Role: Hook for jeep-specific post-render effects; upstream has no behavior.
 * Upstream: vjeep.cpp:198-204
 */
export function doJeepAfterEffects(
  map: unknown,
  destination: unknown,
  shiftX: number,
  shiftY: number,
): void {
  void map;
  void destination;
  void shiftX;
  void shiftY;
}

/**
 * Port of upstream `VLight::DoAfterEffects`.
 * Role: Hook for light-tank-specific post-render effects; upstream has no behavior.
 * Upstream: vlight.cpp:222-228
 */
export function doLightAfterEffects(
  map: unknown,
  destination: unknown,
  shiftX: number,
  shiftY: number,
): void {
  void map;
  void destination;
  void shiftX;
  void shiftY;
}

/**
 * Port of upstream `VMedium::DoAfterEffects`.
 * Role: Hook for medium-tank-specific post-render effects; upstream has no behavior.
 * Upstream: vmedium.cpp:216-222
 */
export function doMediumAfterEffects(
  map: unknown,
  destination: unknown,
  shiftX: number,
  shiftY: number,
): void {
  void map;
  void destination;
  void shiftX;
  void shiftY;
}

/**
 * Port of upstream `VMissileLauncher::DoAfterEffects`.
 * Role: Hook for missile-launcher-specific post-render effects; upstream has no behavior.
 * Upstream: vmissilelauncher.cpp:184-190
 */
export function doMissileLauncherAfterEffects(
  map: unknown,
  destination: unknown,
  shiftX: number,
  shiftY: number,
): void {
  void map;
  void destination;
  void shiftX;
  void shiftY;
}
