/**
 * Upstream: rgrunt.cpp, rlaser.cpp, rpsycho.cpp, rpyro.cpp, rsniper.cpp,
 * rtough.cpp
 */

/**
 * Port of upstream `RGrunt::DoAfterEffects`.
 * Role: Hook for grunt-specific post-render effects; upstream has no behavior.
 * Upstream: rgrunt.cpp:137-140
 */
export function doGruntAfterEffects(
  map: unknown,
  destination: unknown,
  shiftX: number,
  shiftY: number,
): void {
  void map;
  void destination;
  void shiftX;
  void shiftY;
}

/**
 * Port of upstream `RLaser::DoAfterEffects`.
 * Role: Hook for laser-specific post-render effects; upstream has no behavior.
 * Upstream: rlaser.cpp:136-139
 */
export function doLaserAfterEffects(
  map: unknown,
  destination: unknown,
  shiftX: number,
  shiftY: number,
): void {
  void map;
  void destination;
  void shiftX;
  void shiftY;
}

/**
 * Port of upstream `RPsycho::DoAfterEffects`.
 * Role: Hook for psycho-specific post-render effects; upstream has no behavior.
 * Upstream: rpsycho.cpp:136-139
 */
export function doPsychoAfterEffects(
  map: unknown,
  destination: unknown,
  shiftX: number,
  shiftY: number,
): void {
  void map;
  void destination;
  void shiftX;
  void shiftY;
}

/**
 * Port of upstream `RPyro::DoAfterEffects`.
 * Role: Hook for pyro-specific post-render effects; upstream has no behavior.
 * Upstream: rpyro.cpp:136-139
 */
export function doPyroAfterEffects(
  map: unknown,
  destination: unknown,
  shiftX: number,
  shiftY: number,
): void {
  void map;
  void destination;
  void shiftX;
  void shiftY;
}

/**
 * Port of upstream `RSniper::DoAfterEffects`.
 * Role: Hook for sniper-specific post-render effects; upstream has no behavior.
 * Upstream: rsniper.cpp:136-139
 */
export function doSniperAfterEffects(
  map: unknown,
  destination: unknown,
  shiftX: number,
  shiftY: number,
): void {
  void map;
  void destination;
  void shiftX;
  void shiftY;
}

/**
 * Port of upstream `RTough::DoAfterEffects`.
 * Role: Hook for tough-specific post-render effects; upstream has no behavior.
 * Upstream: rtough.cpp:133-136
 */
export function doToughAfterEffects(
  map: unknown,
  destination: unknown,
  shiftX: number,
  shiftY: number,
): void {
  void map;
  void destination;
  void shiftX;
  void shiftY;
}
