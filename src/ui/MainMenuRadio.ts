/**
 * Upstream: gmmw_radio.cpp
 */

/**
 * Port of upstream `lx`.
 * Role: Defines the selector x-offset for the first radio segment.
 * Upstream: gmmw_radio.cpp:97
 */
export const MAIN_MENU_RADIO_LEFT_SELECTOR_OFFSET_PIXELS = 7;

/**
 * Port of upstream `cx`.
 * Role: Defines the selector x-offset for middle radio segments.
 * Upstream: gmmw_radio.cpp:98
 */
export const MAIN_MENU_RADIO_CENTER_SELECTOR_OFFSET_PIXELS = 4;

/**
 * Port of upstream `rx`.
 * Role: Defines the selector x-offset for the last radio segment.
 * Upstream: gmmw_radio.cpp:99
 */
export const MAIN_MENU_RADIO_RIGHT_SELECTOR_OFFSET_PIXELS = 4;
